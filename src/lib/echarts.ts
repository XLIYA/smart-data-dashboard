// ------------------------------
// src/lib/echarts.ts
// ------------------------------
import * as echarts from 'echarts'
export const initChart = (el: HTMLDivElement) => echarts.init(el)
export default echarts
